"""content_factory.platform — shared core used by generation, audit, and catalog.

Home for cross-cutting infrastructure: the LLM gateway (llm), observability,
the exception hierarchy, and — added in later phases — unified config, cache,
prompt registry, db session and shared domain types.
"""
