# Критерии проверки README

Проверка README нужна не для блокировки генерации, а для управляемой доработки. Результат показывается методологу как таблица критериев: пройдено, предупреждения, не пройдено.

## Общая модель

- Всего используется 44 критерия: 39 базовых (разделы 1–4) + 5 сигналов целостности (N.1–N.5).
- Это **структурная ось** (бинарные критерии: есть/нет, качество формы). Рядом с ней работает
  отдельная **дидактическая ось** — жюри моделей оценивает 1–5 «учит ли текст» (связность,
  scaffolding, качество примеров, когнитивная нагрузка, p2p-тон, не-AI-водность). Дидактическая
  ось **не складывается** с 39 критериями — это второй отчёт рядом (`didactic` в report_json).
  Пока opt-in (`DIDACTIC_AXIS_ENABLED`), advisory. Severity — по типу причины: `below_floor`
  дименшен → major, `jury_split` (разброс жюри) → minor. Единственный hard-блок (critical →
  human review) — когда **промотированный** (калибровкой доведённый до enforce) дименшен ниже
  floor; сам по себе `needs_human_review` больше не эскалирует.
- Каждый критерий возвращает статус, комментарий и технические детали для отладки.
- Hard-критерии влияют на итоговую оценку.
- Soft/editorial/semantic-критерии могут отображаться как предупреждения, если нарушение не должно откатывать результат.
- **Авто-калибровка strictness.** Объективные (`script`) критерии и дидактические дименшены
  копят per-criterion статистику с реальных прогонов. Когда критерий стабильно проходит
  (≥50 прогонов, pass-rate ≥95%) — авто-повышается SOFT→HARD (начинает блокировать); при
  просадке recent pass-rate <85% — авто-демоушен обратно. Субъективные критерии (denylist:
  дидактический тон/не-AI-водность, семантика) не промотятся. Всё под аудит-логом
  (`artifacts/strictness_state.json`). Два флага: `CALIBRATION_ENABLED` (запись+состояние),
  `CALIBRATION_ENFORCE` (реальная блокировка) — оба opt-in (сначала shadow-накопление).
- Отсутствие необязательного контекста не должно превращаться в fail.

## Группы критериев

| Группа | Что проверяет |
|---|---|
| Структура документа | Заголовок, содержание, главы, заключение, Markdown-ссылки. |
| Глава 1 | Введение, инструкция, понятность входа в проект. |
| Глава 2 | Теоретический блок, объем разделов, определения, примеры, связь с проектом. |
| Глава 3 | Практические задания, ожидаемые результаты, артефакты, p2p-проверяемость. |
| Сторителлинг | Единый нарративный фокус и связность между главами. |
| Финальное завершение | Заключение завершает текущий проект без анонса следующего. |
| Целостность (N.1–N.5) | Таблицы не слиты с прозой, нет дословных повторов и болванок, диаграммы по теме раздела, нет оборванных кавычек, единый идентификатор проекта. Пока advisory (SOFT/предупреждение), калибруются на корпусе. |

## Образовательные результаты

Отдельное обязательное поле с знаниями, умениями и навыками больше не используется. Если в контексте есть образовательные результаты, критерий 2.4.5 использует их как дополнительный сигнал. Если образовательные результаты не переданы, критерий не должен снижать итоговую оценку; максимум — предупреждение о том, что дополнительный контекст отсутствует.

## Как читать статусы

| Статус | Значение |
|---|---|
| Пройден | Критерий выполнен. |
| Предупреждение | Есть риск качества, но это не повод откатывать генерацию автоматически. |
| Не пройден | Нарушение мешает принять README без правки. |

## Как использовать в перегенерации

Непройденные критерии можно передать в запрос правки, но запрос должен оставаться содержательным:

```text
Исправь непройденные критерии по главе 2 без изменения структуры проекта.
Сохрани количество задач и не добавляй новые главы.
```

Если правка добавляет или удаляет главу, разрешены связанные изменения:

- содержание;
- номера заголовков;
- Markdown-ссылки;
- rubric/metrics после пересчета.

## Для разработчиков

Основные файлы:

- `content_gen/validators/rubric/scorer.py`
- `content_gen/validators/rubric/chapter1_checker.py`
- `content_gen/validators/rubric/chapter2_checker.py`
- `content_gen/validators/rubric/chapter3_checker.py`
- `content_gen/validators/rubric/section3_checker.py`
- `content_gen/models/criteria_models.py`

Перед изменением критериев запускайте:

```powershell
python -m pytest tests/validators/rubric -q
python -m pytest tests/utils/test_static_ui_contracts.py -q
```
